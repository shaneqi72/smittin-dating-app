// type = {
//   onClick: () => void;
//   backgroundColor: string;
//   isSelected: Boolean;
//   titleColor: string;
// }

// export const Button = (props) => {
//   const { onClick, isSelected, backgroundColor, titleColor } = props;

//   return (
//     <div className={classnames(styles.buttonBase, isSelected ? 'red' : 'blue')}>
//       {/* <Paper
//                 elevation={1}
//                 className={
//                     ? `${classes.paper} ${classes.clickedOption}`
//                     : `${classes.paper}`
//                 }
//                 onClick={() => chooseTheme(item.type)}
//               >
//                 <Typography variant="h4">{item.type}</Typography>
//               </Paper> */}
//         <ToggleButton
//                 isSelected={selectedOption.theme === item.type}
//                 onSelect={() => chooseTheme(item.type)}

//               />

//     </div>
//   )
// }
